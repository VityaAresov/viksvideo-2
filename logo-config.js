// Option 1: paste your preferred logo as a data URI below to override the default mark.
// Example: window.VIKS_LOGO_DATA_URI = 'data:image/png;base64,iVBORw0KGgoAAAANS...';
window.VIKS_LOGO_DATA_URI = '';

// Option 2: if you have raw SVG markup, drop it into the string below.
// Example: window.VIKS_LOGO_INLINE_SVG = '<svg xmlns="http://www.w3.org/2000/svg" ...>...</svg>';
// We'll encode it at runtime so you don't need to convert it manually.
window.VIKS_LOGO_INLINE_SVG = '';

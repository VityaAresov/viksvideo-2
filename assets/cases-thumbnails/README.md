# Case thumbnails

Add custom thumbnail images for the Cases page here. Reference the files from `cases-config.js` by setting the `thumbnail` field, e.g. `thumbnail: 'assets/cases-thumbnails/your-image.jpg'`.
